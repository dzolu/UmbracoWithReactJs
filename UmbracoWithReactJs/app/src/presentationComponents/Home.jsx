import React from 'react';
import PropTypes from 'prop-types';

const Home = (props) => {
    return (<div>Home</div>)
};

Home.PropTypes = {
    myPropTypes: PropTypes.string.isRequired
};

export default Home;